spring.application.name=login-utils-mgr

# ??? Port?Cloud Run ????? PORT ?????????????? 8081
server.port=${PORT}

# ???????????????? Tailscale IP??????? GCP ??????
spring.datasource.url=jdbc:mysql://${DB_HOST}:3306/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
# ???????????????????????
spring.datasource.password=${DB_PASSWORD}

mybatis-plus.mapper-locations=classpath*:org/loginutils/dal/mappers/*.xml
mybatis-plus.type-aliases-package=org.loginutils.dal.model

# Redis ??????? Redis ??? Cloud Run ???????????? REDIS_HOST ??
spring.data.redis.host=${REDIS_HOST}
spring.data.redis.port=6379

# Email ?? (Gmail ??)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# ?????????????????? GCP Cloud Run ?? Secret Manager ???????
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ???????
system.mgr.data=R20260312
system.mgr.version=v2.0.0